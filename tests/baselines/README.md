# Historical renderer evidence

These captured SVG/JSON files belong to the deleted rendering engine. They are retained only for historical review and fixture development, and are not used as expected output for DxfSkia. The Skia gate uses independent geometry assertions and real native pixels. No AutoCAD/TrueView or old-engine parity claim follows from these files.
